apiVersion: v2
name: taskflow
description: TaskFlow REST API — Spring Boot + PostgreSQL task management service
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "15.5.17"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
